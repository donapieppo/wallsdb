<h1><%= @event.wall %> <%= link_to_edit([:edit, @event]) if current_user_owns?(@event) %></h1>

<%= render @event, complete: true %>  

<div class="row">
<% @other_events.each do |event| %>  
  <div class="col-md-4 col-md-offset-4">
    <%= render event %>  
  </div>
<% end %>
</div>
